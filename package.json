{
  "name": "next-hono-backend",
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy --minify src/index.ts"
  },
  "dependencies": {
    "hono": "^3.12.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "prettier": "^3.1.1",
    "wrangler": "^3.22.0"
  }
}
